@model YourTurn.Web.Controllers.LobbyController.LobbyRoomViewModel

@{
    ViewData["Title"] = "Lobi";
    var currentPlayerName = ViewBag.CurrentPlayerName?.ToString();
    var currentPlayer = Model.Lobby.Players.FirstOrDefault(p => p.Name == currentPlayerName);
    var currentPlayerTeam = currentPlayer?.Team ?? "";
    var isHost = Model.Lobby.HostPlayerName == ViewBag.CurrentPlayerName?.ToString();
    var gameState = Model.Lobby.GameState ?? new GameState { IsWaitingForVolunteers = true };
    var team1Volunteer = gameState.Team1Volunteer;
    var team2Volunteer = gameState.Team2Volunteer;
}

@Html.AntiForgeryToken()

<div id="lobby-data" 
    data-lobby-code="@Model.Lobby.LobbyCode" 
    data-current-player-name="@currentPlayerName"
    data-host-player-name="@Model.Lobby.HostPlayerName">
</div>

<div class="d-flex justify-content-between align-items-center mb-3">
    <form method="post" action="/Lobby/Leave" class="d-inline">
        <input type="hidden" name="code" value="@Model.Lobby.LobbyCode" />
        <button type="submit" class="btn btn-outline-secondary">
            <i class="fas fa-arrow-left"></i> Lobiden Ayrıl
        </button>
    </form>
    <h2 class="text-center mb-0">Lobi Kodu: <span id="lobbyCode" class="text-warning" style="cursor: pointer; user-select: all;" onclick="copyLobbyCode()" title="Kopyalamak için tıklayın">@Model.Lobby.LobbyCode</span></h2>
    <div style="width: 140px;"></div> <!-- Spacing için boş div -->
</div>

@if (isHost && !Model.Lobby.IsGameStarted)
{
    <div class="row mb-4 justify-content-center">
        <!-- Takım Seçmeyen Oyuncular Card -->
        <div class="col-md-4 d-flex justify-content-end">
            <div class="card w-100 border-secondary">
                <div class="card-body p-2">
                    @if (Model.Lobby.Players.Any(p => string.IsNullOrEmpty(p.Team)))
                    {
                        <ul class="list-group mb-0">
                            @foreach (var player in Model.Lobby.Players.Where(p => string.IsNullOrEmpty(p.Team)))
                            {
                                <li class="list-group-item d-flex justify-content-between align-items-center">
                                    @player.Name
                                    @if (player.Name == currentPlayerName)
                                    {
                                        <span class="badge bg-success">Sen</span>
                                    }
                                </li>
                            }
                        </ul>
                    }
                    else
                    {
                        <div class="text-center text-muted py-3">Seyirci Yok</div>
                    }
                </div>
            </div>
        </div>
        <!-- Oyun Yönetimi & Kategori Card -->
        <div class="col-md-5 d-flex justify-content-start">
            <div class="card mb-4" style="max-width: 600px; width:100%">
                <div class="card-body">
                    <!-- Kategori Seçimi (küçültülmüş ve yatay) -->
                    <div class="row align-items-center mb-2">
                        <div class="col-auto">
                            <label for="categorySelect" class="col-form-label mb-0"><strong>Kategori:</strong></label>
                        </div>
                        <div class="col">
                            @if (string.IsNullOrEmpty(Model.Lobby.Category))
                            {
                                <form method="post" action="/Lobby/ChooseCategory" class="d-flex gap-2">
                                    <input type="hidden" name="code" value="@Model.Lobby.LobbyCode" />
                                    <select class="form-select form-select-sm w-auto" name="category" id="categorySelect" required onchange="this.form.submit()">
                                        <option value="">Seçin</option>
                                        @foreach (var category in Model.Categories)
                                        {
                                            <option value="@category.Name">@category.Name</option>
                                        }
                                    </select>
                                </form>
                            }
                            else
                            {
                                <span class="badge bg-success fs-6">@Model.Lobby.Category</span>
                                <form method="post" action="/Lobby/ChooseCategory" class="d-inline ms-2">
                                    <input type="hidden" name="code" value="@Model.Lobby.LobbyCode" />
                                    <input type="hidden" name="category" value="" />
                                    <button type="submit" class="btn btn-sm btn-outline-secondary">Değiştir</button>
                                </form>
                            }
                        </div>
                    </div>
                    <!-- Takım Yönetimi (kompakt) -->
                    <div class="row g-2 align-items-center">
                        <div class="col">
                            <form method="post" action="/Lobby/RandomizeTeams" class="d-inline">
                                <input type="hidden" name="code" value="@Model.Lobby.LobbyCode" />
                                <button type="submit" class="btn btn-success btn-sm w-100" onclick="return confirm('Oyuncuları rastgele takımlara dağıtmak istediğinizden emin misiniz?')">
                                    <i class="fas fa-random"></i> Rastgele Dağıt
                                </button>
                            </form>
                        </div>
                        <div class="col">
                            <form method="post" action="/Lobby/ResetTeams" class="d-inline">
                                <input type="hidden" name="code" value="@Model.Lobby.LobbyCode" />
                                <button type="submit" class="btn btn-warning btn-sm w-100" onclick="return confirm('Tüm takım atamalarını sıfırlamak istediğinizden emin misiniz?')">
                                    <i class="fas fa-undo"></i> Takımları Sıfırla
                                </button>
                            </form>
                        </div>
                    </div>
                    <div class="mt-2">
                        <small class="text-muted">
                            <i class="fas fa-info-circle"></i> Rastgele dağıtma için en az 2 takım seçmeyen oyuncu olması gerekiyor.
                        </small>
                    </div>
                </div>
            </div>
        </div>
    </div>
}
else if (!string.IsNullOrEmpty(Model.Lobby.Category))
{
    <p class="text-center text-muted">Kategori: <strong class="text-success">@Model.Lobby.Category</strong></p>
}
else
{
    <p class="text-center text-muted">Kategori: <em class="text-warning">Host tarafından seçilmeyi bekliyor...</em></p>
}

<!-- Hata ve Başarı Mesajları -->
@if (TempData["Error"] != null)
{
    <div class="alert alert-danger alert-dismissible fade show" role="alert">
        <strong>Hata:</strong> @TempData["Error"]
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    </div>
}

@if (TempData["Success"] != null)
{
    <div class="alert alert-success alert-dismissible fade show" role="alert">
        <strong>Başarılı:</strong> @TempData["Success"]
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
    </div>
}

<div class="row mt-4">
    <!-- Kırmızı Takım (Sol) -->
    <div class="col-md-6 d-flex justify-content-end">
        <div class="card w-75 border-danger">
            <div class="card-header bg-danger text-white text-center">
                <h4 class="mb-0">🔴 Kırmızı Takım</h4>
            </div>
            <div class="card-body d-flex flex-column justify-content-between" style="min-height: 350px;">
                <!-- Takım oyuncuları listesi -->
                <ul class="list-group mb-3">
                    @foreach (var player in Model.Lobby.Players.Where(p => p.Team == "Sol"))
                    {
                        <li class="list-group-item">
                            <div class="d-flex justify-content-between align-items-center">
                                <span>@player.Name</span>
                                <div>
                                    @if (player.Name == currentPlayerName)
                                    {
                                        <span class="badge bg-success">Sen</span>
                                    }
                                    @if (Model.Lobby.GameState?.Team1Volunteer == player.Name)
                                    {
                                        <span class="badge bg-danger">Gönüllü</span>
                                    }
                                </div>
                            </div>
                            @if (player.Name == currentPlayerName && (string.IsNullOrEmpty(team1Volunteer) || team1Volunteer == currentPlayerName))
                            {
                                <div class="mt-2 text-center">
                                    @if (team1Volunteer != currentPlayerName)
                                    {
                                        <button class="btn btn-sm btn-danger" onclick="volunteerForTeam('Sol')">
                                            <i class="fas fa-hand-paper"></i> Gönüllü Ol
                                        </button>
                                    }
                                    else
                                    {
                                        <button class="btn btn-sm btn-warning" onclick="withdrawVolunteer('Sol')">
                                            <i class="fas fa-times"></i> Gönüllülükten Çık
                                        </button>
                                    }
                                </div>
                            }
                        </li>
                    }
                </ul>
                <!-- Altta Takıma Katıl butonu veya bilgi -->
                <div class="text-center mt-auto">
                    @if (!Model.Lobby.IsGameStarted)
                    {
                        if (currentPlayerTeam != "Sol")
                        {
                            <form method="post" action="/Lobby/ChooseTeam">
                                <input type="hidden" name="code" value="@Model.Lobby.LobbyCode" />
                                <input type="hidden" name="playerName" value="@currentPlayerName" />
                                <input type="hidden" name="team" value="Sol" />
                                <button type="submit" class="btn btn-danger">Bu Takıma Katıl</button>
                            </form>
                        }
                    }
                    else
                    {
                        <p class="text-muted">Skor: @Model.Lobby.GameState.Team1Score</p>
                    }
                </div>
            </div>
        </div>
    </div>
    <!-- Mavi Takım (Sağ) -->
    <div class="col-md-6 d-flex justify-content-start">
        <div class="card w-75 border-primary">
            <div class="card-header bg-primary text-white text-center">
                <h4 class="mb-0">🔵 Mavi Takım</h4>
            </div>
            <div class="card-body d-flex flex-column justify-content-between" style="min-height: 350px;">
                <!-- Takım oyuncuları listesi -->
                <ul class="list-group mb-3">
                    @foreach (var player in Model.Lobby.Players.Where(p => p.Team == "Sağ"))
                    {
                        <li class="list-group-item">
                            <div class="d-flex justify-content-between align-items-center">
                                <span>@player.Name</span>
                                <div>
                                    @if (player.Name == currentPlayerName)
                                    {
                                        <span class="badge bg-success">Sen</span>
                                    }
                                    @if (Model.Lobby.GameState?.Team2Volunteer == player.Name)
                                    {
                                        <span class="badge bg-primary">Gönüllü</span>
                                    }
                                </div>
                            </div>
                            @if (player.Name == currentPlayerName && (string.IsNullOrEmpty(team2Volunteer) || team2Volunteer == currentPlayerName))
                            {
                                <div class="mt-2 text-center">
                                    @if (team2Volunteer != currentPlayerName)
                                    {
                                        <button class="btn btn-sm btn-primary" onclick="volunteerForTeam('Sağ')">
                                            <i class="fas fa-hand-paper"></i> Gönüllü Ol
                                        </button>
                                    }
                                    else
                                    {
                                        <button class="btn btn-sm btn-warning" onclick="withdrawVolunteer('Sağ')">
                                            <i class="fas fa-times"></i> Gönüllülükten Çık
                                        </button>
                                    }
                                </div>
                            }
                        </li>
                    }
                </ul>
                <!-- Altta Takıma Katıl butonu veya bilgi -->
                <div class="text-center mt-auto">
                    @if (!Model.Lobby.IsGameStarted)
                    {
                        if (currentPlayerTeam != "Sağ")
                        {
                            <form method="post" action="/Lobby/ChooseTeam">
                                <input type="hidden" name="code" value="@Model.Lobby.LobbyCode" />
                                <input type="hidden" name="playerName" value="@currentPlayerName" />
                                <input type="hidden" name="team" value="Sağ" />
                                <button type="submit" class="btn btn-primary">Bu Takıma Katıl</button>
                            </form>
                        }
                    }
                    else
                    {
                        <p class="text-muted">Skor: @Model.Lobby.GameState.Team2Score</p>
                    }
                </div>
            </div>
        </div>
    </div>
</div>

@if (!Model.Lobby.IsGameStarted && !string.IsNullOrEmpty(currentPlayerTeam))
{
    <div class="text-center mt-3">
        <form method="post" action="/Lobby/LeaveTeam" class="d-inline">
            <input type="hidden" name="code" value="@Model.Lobby.LobbyCode" />
            <input type="hidden" name="playerName" value="@currentPlayerName" />
            <button type="submit" class="btn btn-warning">Takımdan Çık</button>
        </form>
    </div>
}

@if (isHost && !Model.Lobby.IsGameStarted)
{
    <div class="mt-4 text-center">
        <form method="post" action="/Lobby/StartGame">
            <input type="hidden" name="code" value="@Model.Lobby.LobbyCode" />
            <button type="submit" class="btn btn-lg btn-success w-50 shadow">
                <i class="fas fa-play-circle"></i> OYUNU BAŞLAT
            </button>
        </form>
    </div>
}

@section Scripts {
    <script src="https://cdnjs.cloudflare.com/ajax/libs/microsoft-signalr/8.0.0/signalr.min.js"></script>
    <script src="/js/peer-hosting.js"></script>
    <script src="~/js/lobby.js" asp-append-version="true"></script>
}